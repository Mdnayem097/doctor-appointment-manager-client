export const metadata = {
  title: "Login | DocAppoint",
  description: "Login to your account to access dashboard and manage appointments",
};

import LoginClient from "./LoginClient";

export default function Page() {
  return <LoginClient />;
}